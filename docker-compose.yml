version: '3'
services:
  application:
    container_name: storage.backup
    build: .
    image: storage.backup:latest
    restart: unless-stoped
    env_file: .env
    volumes:
      - ./:/opt/app
    ports:
      - '9000:9000'
